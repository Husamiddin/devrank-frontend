# Windows quick start

```bat
cd devrank-frontend
npm install
npm start
```

The frontend expects the backend at `http://localhost:5000` by default.

For a different API URL, create `.env` from `.env.example`:
```env
REACT_APP_API_URL=http://localhost:5000
```
